# Phase 4 Scheduled Project Health

Scheduled project health checks were added in `.github/workflows/scheduled-project-health.yml`.

The workflow is read-only and uploads one evidence artifact per project adapter.
